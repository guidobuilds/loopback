{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Checking for generated-artifact edits",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */setup/dist/*|*/setup/mcp-bundle/*) jq -n '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Generated artifact. Edit the source under loopback/skills or loopback/commands and rebuild (cd setup && npm run build) — do not edit setup/dist/ or setup/mcp-bundle/ directly.\"}}';; esac"
          }
        ]
      }
    ]
  }
}
