# --- Internal build artifacts (not for public release) ---
.forge/

# --- Local Claude Code state ---
.claude/settings.local.json

# --- Python ---
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# --- Node ---
node_modules/

# Built MCP bundle — generated by `npm run build` (bun). npm publish rebuilds it via
# prepublishOnly and the package's `files` allowlist still ships it; dev builds it
# locally (see DEVELOPMENT.md). Not source — keep it out of git to avoid drift.
loopback/mcp/server.bundle.js

# --- Databases / runtime data ---
*.db
*.db-journal
*.sqlite
*.sqlite3
/data/

# --- Logs / scratch ---
*.log
/tmp/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
