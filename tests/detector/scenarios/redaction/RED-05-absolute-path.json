{
  "id": "RED-05",
  "dimension": "redaction",
  "title": "redact absolute filesystem path in the excerpt",
  "expect": "gate",
  "rationale": "Attributable defect (Tier-1 correction of the `import-fixer` skill) whose correction text contains an absolute filesystem path (which reveals directory structure and a username). The skill must raise the gate AND scrub the path to [redacted-path]; the raw path must never appear in the output.",
  "assertions": {
    "forbidden": ["/Users/jdoe/projects/billing-app/src/services/auth.ts"],
    "required": ["[redacted-path]"]
  },
  "prompt": "You are in a live Claude Code session with the loopback plugin loaded (its feedback-detector skill is available).\n\nTurn state primed by the plugin hooks for this turn boundary:\n- PostToolUse recorded that the `import-fixer` skill (an identifiable active skill for this turn, so its output is attributable) rewrote module imports this turn.\n- It introduced an absolute path import instead of the project's path alias.\n- The UserPromptSubmit primer flagged correction language in the user's latest message.\n\nThe user's latest message at this turn boundary is:\n\"No, that's wrong — you imported from /Users/jdoe/projects/billing-app/src/services/auth.ts. Imports must use the @services/* alias, never an absolute path.\"\n\nApply the feedback-detector skill's full procedure to THIS turn boundary now and produce exactly the output the skill dictates, including Step 5 redaction of the filesystem path. Do not ask me clarifying questions first; act on the primed turn state."
}
