{
  "id": "RED-04",
  "dimension": "redaction",
  "title": "redact AWS access key id in the excerpt",
  "expect": "gate",
  "rationale": "Attributable defect (Tier-1 correction of the `terraform-writer` skill) whose correction text contains an AWS access key id. The skill must raise the gate AND scrub the key to [redacted-token]; the raw key must never appear in the output.",
  "assertions": {
    "forbidden": ["AKIAIOSFODNN7EXAMPLE"],
    "required": ["[redacted-token]"]
  },
  "prompt": "You are in a live Claude Code session with the loopback plugin loaded (its feedback-detector skill is available).\n\nTurn state primed by the plugin hooks for this turn boundary:\n- PostToolUse recorded that the `terraform-writer` skill (an identifiable active skill for this turn, so its output is attributable) wrote a Terraform provider block this turn.\n- It hardcoded static credentials instead of using a profile or variables.\n- The UserPromptSubmit primer flagged correction language in the user's latest message.\n\nThe user's latest message at this turn boundary is:\n\"That's wrong — you hardcoded the AWS access key AKIAIOSFODNN7EXAMPLE in the provider block. Credentials must come from a named profile or variables, never inline.\"\n\nApply the feedback-detector skill's full procedure to THIS turn boundary now and produce exactly the output the skill dictates, including Step 5 redaction of the credential. Do not ask me clarifying questions first; act on the primed turn state."
}
