{
  "id": "PREC-03",
  "dimension": "precision",
  "title": "iteration, additive scope (handle the empty case too)",
  "expect": "silent",
  "rationale": "Asking the skill to also handle an additional case ('also handle the empty-list case') is additive scope, not a repair of what was produced. The defect-vs-iteration filter classifies it as iteration -> stay silent.",
  "assertions": { "forbidden": [], "required": [] },
  "prompt": "You are in a live Claude Code session with the loopback plugin loaded (its feedback-detector skill is available).\n\nTurn state primed by the plugin hooks for this turn boundary:\n- PostToolUse recorded that the `endpoint-writer` skill (an identifiable active skill for this turn) implemented a list endpoint this turn that returns results correctly for the populated case.\n\nThe user's latest message at this turn boundary is:\n\"Looks good. Also handle the empty-list case so it returns [] instead of null.\"\n\nApply the feedback-detector skill's full procedure to THIS turn boundary now and produce exactly the output the skill dictates. Apply the defect-vs-iteration filter to this additive request. Do not ask me clarifying questions first; act on the primed turn state."
}
