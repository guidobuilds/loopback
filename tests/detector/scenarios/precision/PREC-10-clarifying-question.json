{
  "id": "PREC-10",
  "dimension": "precision",
  "title": "not a correction (user asks a clarifying question)",
  "expect": "silent",
  "rationale": "The user asks a question about the skill's output rather than correcting it. A question is not a Tier-1 correction, a revert, or repeated re-instruction -> no candidate -> stay silent.",
  "assertions": { "forbidden": [], "required": [] },
  "prompt": "You are in a live Claude Code session with the loopback plugin loaded (its feedback-detector skill is available).\n\nTurn state primed by the plugin hooks for this turn boundary:\n- PostToolUse recorded that the `query-builder` skill (an identifiable active skill for this turn) wrote a SQL query this turn that returns the expected rows.\n\nThe user's latest message at this turn boundary is:\n\"Why did you use a LEFT JOIN here instead of an INNER JOIN?\"\n\nApply the feedback-detector skill's full procedure to THIS turn boundary now and produce exactly the output the skill dictates. A clarifying question is not correction language; no Tier-1 signal is present. Do not ask me clarifying questions first; act on the primed turn state."
}
