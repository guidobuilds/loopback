{
  "id": "FP3",
  "title": "muted skill (same defect as TP1, but prd-writer is muted)",
  "expect": "silent",
  "mute": "prd-writer",
  "rationale": "Identical to TP1 (a real defect with a clear Tier-1 correction), EXCEPT the user previously chose 'Never for this skill', so prd-writer is muted on this machine. The mute gate must short-circuit and stay silent -> NO consent gate.",
  "prompt": "You are in a live Claude Code session with the loopback plugin loaded (its feedback-detector skill is available).\n\nTurn state primed by the plugin hooks for this turn boundary:\n- PostToolUse recorded a Write tool call this turn that created the file prd.md.\n- That output was produced by the `prd-writer` skill, which is an identifiable active skill for this turn (so the correction is attributable).\n- IMPORTANT: prd-writer is MUTED on this machine (the user previously chose 'Never for this skill'). You MUST verify this by running the mute gate exactly as the skill instructs: call the `is_muted` tool (loopback MCP) with prd-writer. It will return `muted: true`.\n- The UserPromptSubmit primer flagged correction language in the user's latest message.\n\nThe user's latest message at this turn boundary is:\n\"No, that's the wrong format. The PRD template is Problem/Solution/Metrics, not the freeform structure you used in prd.md.\"\n\nApply the feedback-detector skill's full procedure to THIS turn boundary now and produce exactly the output the skill dictates. Run the mute gate via the is_muted tool; if it reports muted, stay silent as the skill requires. Do not ask me clarifying questions first; act on the primed turn state."
}
